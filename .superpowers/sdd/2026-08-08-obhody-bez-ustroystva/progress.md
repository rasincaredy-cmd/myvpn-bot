# SDD ledger — plan: docs/superpowers/plans/2026-08-08-obhody-bez-ustroystva.md
Ветка obhody-bez-ustroystva от d7fa00f, worktree .claude/worktrees/wdtt-standalone (параллельно в main работает сессия МЕНЕДЖЕР)
Приёмка: tests/test_qrgen.py падает без PIL — критерий «зелёное» = нет НОВЫХ падений
Задача 1 DONE (765771c) — _ask_device пропускает шаг при нуле устройств, _standalone_label «Резервное подключение N» (наименьший свободный номер), device_id=None по всему пути создания. 4 теста, обе канарейки роняют
Задача 2 DONE (00e4ab8) — repo.delete_device: UPDATE device_id=NULL вместо DELETE; teardown больше не ходит на сервер обхода. 2 теста, откат обеих правок роняет
Задача 3 DONE (eb76e17) — отзыв и ревайв перебирают подключения по юзеру. Отзыв снимает с сервера ДО revoke_device (иначе активных уже не осталось бы), отвязанные гасит адресно + пишет в журнал. Ревайв вынесен в _revive_access и не зависит от лимита устройств. user_wipe перестал дублировать отзыв отвязанных. 4 теста, обе канарейки роняют
Задача 3: контракт `cleared` от сессии МЕНЕДЖЕР соблюдён — id в набор кладётся только после успешного SSH, строка с неснятым паролем не удаляется
Задача 4 DONE (11c5474) — wdtt_pick_device переписан (устройство = имя, а не пара), «Устройство: {label}» → «Название: {label}», сообщение об удалении устройства говорит про уцелевшее подключение только когда оно было. README: отвязка + почему метка снимается явным UPDATE. +2 теста
Задача 4: карточку подключения НЕ трогал — её заголовок и есть метка устройства, отдельная строка «Устройство: X» повторяла бы заголовок
Швы проверены по коду: секция 2a планировщика (stale wdtt) отбирает по статусу и дате, устройство ей не нужно; 2b (зомби-устройства) после отвязки не держится за подключение; истечение подписки в секции 1 отбирает ЮЗЕРОВ, а не владельцев устройств, — юзер с одним подключением теперь и отзывается, и получает уведомление; админский список обходов юзера идёт по list_wdtt_for_user, отвязанные админу видны; админское удаление устройства наследует новое поведение через teardown
Приёмка: 405 зелёных, падают только 2 предсуществующих (PIL). Страж формулировок 45/45
